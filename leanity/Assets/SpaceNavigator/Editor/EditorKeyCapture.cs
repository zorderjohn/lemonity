using UnityEditor;
using UnityEngine;
/*
[CustomEditor(typeof(LeapTestCalc))]
public class EditorKeyCapture : Editor
{
    void OnSceneGUI()
    {
        //VertexPaint script = (VertexPaint)target;
        Event e = Event.current;
        switch (e.type)
        {
            case EventType.keyDown:
                {
                    if (Event.current.keyCode == (KeyCode.A))
                    {
                        Debug.Log("Espaciando...");
                    }
                    break;
                }
            default:
                Debug.Log(e.type);
                break;
        }
    }
}*/